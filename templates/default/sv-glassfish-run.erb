#!/bin/sh
#
# Generated by Chef
#
exec 2>&1
umask 0077
<% @options[:resource].env_variables.sort.each do |key, value| -%>
export <% key %>=<% value %>
<% end %>
cd <%= node['glassfish']['domains_dir']%>/<%= @options[:resource].domain_name %>/config
exec chpst -u <%= node['glassfish']['user'] %>:<%= node['glassfish']['group'] %> <%= @options[:authbind] ? "/usr/bin/authbind --deep " : "" %><%= node["java"]["java_home"] %>/bin/java <%= @options[:args].join(' ') %> 1>&2 >> /dev/null
